<% # TODO: make this switch based on the status and payment method presence. Need to handle trialing, trialing with payment method, canceled, active to start. %>
<% if current_company.subscription.default_payment_method.blank? %>
  <%= render(Settings::Billing::NeedsSubscriptionComponent.new(company: current_company)) %>
<% else %>
  <%= render(Settings::Billing::SubscriptionDetailsComponent.new(company: current_company)) %>
<% end %>