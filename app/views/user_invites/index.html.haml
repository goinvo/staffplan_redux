%h1 Invitations

%table
  %tr
    %th Company
    %th Respond

  - @invites.each do |invite|
    - if invite.aasm.current_state == :new
      %tr
        %td= invite.company.name
        %td= link_to "Accept", user_invite_accept_path(current_user, invite), method: :accept
        %td= link_to "Decline", user_invite_decline_path(current_user, invite), method: :decline, data: {confirm: "Are you sure?" }
