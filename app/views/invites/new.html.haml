%h1 Send an Invitation

= form_for [@company, @invite] do |f|
  - if @invite.errors.any?
    - @invite.errors.full_messages.each do |msg|
      = msg
  = f.label :email
  = f.email_field :email
  = f.submit "Send Invitation"
